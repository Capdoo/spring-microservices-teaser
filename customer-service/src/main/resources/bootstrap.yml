#server.port = 8081
#spring.application.name=customer-service
#spring.application.cloud.config.uri=http://localhost:8083
#spring.application.cloud.config.username=root
#spring.application.cloud.config.password=secret

#server:
#  port: 8080

spring:
  application:
    name: customer-service-prop
  cloud:
    config:
      #Set config server
      uri: http://localhost:8083
      username: root
      password: secret