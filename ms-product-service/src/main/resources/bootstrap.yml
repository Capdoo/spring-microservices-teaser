spring:
  application:
    name: product-service-prop
  cloud:
    config:
      #Set config server
      uri: http://localhost:8083
      username: root
      password: secret
  boot:
    admin:
      client:
        url: http://localhost:8086